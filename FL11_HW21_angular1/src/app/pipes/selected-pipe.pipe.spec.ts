import { SelectedPipePipe } from './selected-pipe.pipe';

describe('SelectedPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedPipePipe();
    expect(pipe).toBeTruthy();
  });
});
